from fastapi import APIRouter

# Placeholder: currently, project storage endpoints may be embedded in other routers.
# We'll add includes here when those endpoints are modularized.

router = APIRouter()
