/* eslint-env node */
// Tailwind removed: project standardizes on Bootstrap 5.x utilities only.
// Keep PostCSS config minimal to avoid accidental Tailwind re-introduction.
module.exports = { plugins: [] }
